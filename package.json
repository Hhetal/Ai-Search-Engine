{
  "name": "ai-search-engine",
  "version": "1.0.0",
  "description": "AI Search Engine with Frontend, Backend, and Admin Panel",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run admin\" \"npm run frontend\"",
    "backend": "cd backend && npm start",
    "admin": "cd admin-panel && npm start",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../admin-panel && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
